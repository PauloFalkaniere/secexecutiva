// Configuração do Supabase — Secretaria Executiva
//
// Preencha com os dados do SEU projeto Supabase (veja o README.md,
// "Passo 4 — Pegar a URL e a chave do projeto").
//
// Este arquivo fica separado do index.html DE PROPÓSITO: sempre que
// você atualizar o index.html para uma versão nova do sistema, é só
// substituir aquele arquivo — este aqui (config.js) não muda, e suas
// chaves continuam configuradas sem precisar copiar e colar de novo.
//
// Deixando os dois campos em branco (""), o sistema funciona no modo
// local (dados salvos só neste navegador, sem login).

window.SECEX_SUPABASE_URL = "";
window.SECEX_SUPABASE_ANON_KEY = "";
